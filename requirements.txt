Django==4.0.4 # This is for installing the django app
pillow==9.1.0 # This is for the imagefield to work
python-decouple==3.6 # This is for protecting secret environment variables
# windows-curses==2.3.0 # This is for compatibity of nltk with windows
syllables==1.0.3 # This is for syllables library to get syllables of words
pronouncing==0.2.0 # This is for the syllable of a word
pyphen==0.12.0 # This returns the syllable of a word
cmudict==1.0.2 # This is a library of words
djangorestframework==3.13.1 # This is for APIs
djangorestframework-simplejwt==5.1.0 # This is for API for generating auth tokens
drf-yasg==1.20.0 # This is for swagger
psycopg2-binary==2.9.1 # This is for connecting postgresql database
python-docx==0.8.11 # This is for exporting poem to microsoft document
google-api-python-client==2.55.0 # required for google authentication
django-filter==22.1 # required for filtering views
